from setuptools import setup

setup(
	name = 'baseCalculator',
	version = '1.0',
	packages = ['baseCalculator'],
	description = 'It is developed for the ease of conversion of any base to any base. \
	               It is entirely written in pure python',
	) 